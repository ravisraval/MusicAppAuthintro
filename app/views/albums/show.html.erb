<h2> Album: <%= @album.name %> </h2>
<h3> By <%= @album.band.name %> </h3>

<h4> Make a <%= link_to "new track", new_album_track_url(album_id: @album.id) %>.</h4>

<% @album.tracks.each do |track| %>
  <%= link_to track.name, track_url(track)  %>
  <br>
<% end %>



<%= link_to "Back to #{@album.band.name}'s page.", band_url(@album.band) %>
